#------------Day-29----------------
   #----------------Ansible------------------------
   
   ```yaml
       - name : Comp;ete Deployment setup
         hosts : all
	 become : yes
	 tasks ;
	    - name :  Deploy Nginx, MySQL,  and Redis  with docker 
	      command : docker-compose -f /path/to/docker-compose.yml up -d

   #--------Terraform--------------

     respurce "aw_vpc" "pathnex_vpc" {
        cidr_block = "10.0.0.0/16"
	}

	resource "aws_instance" "pathnex_app" {
	   ami = "ami-89809"
	   instance_type = "t2.medium"
	   subnet_is = aws_subnet.pathnex_subnet.id
	   security_groups = [aws_security_group.pathnex_sg.name]
	   }


#---------Kubernetes------------------

    apiVersion : apps/v1
    kind : Deployment
    metadata :
       name : pathnex-app
     spec :
       replics : 2
       selector :
          matchLabels :
	     app : pathnex-app
	  template : metadata 
	     labels:
	        app : pathnex-app
           spec :
	      containers:
	         - name : nginx
		   image : nginx
		   ports :
		     - containerPorts : 80

---
   apiVersion :  v1
   kind : service
   metadata :
     name : pathnex-service
    spec :
       selector :
          app : pathnex-app
	ports :  
	  - port : 80
	  targetPort :


  #-----------Docker-----------
    FROM container prune
    RUN useradd pathnexuser
    USER pathnexuser


    docker container prune
    docker volume prune
    docker volume prune
    docker network prunr
    docker system prune -a


    #------------Final Docker Project- ------------

      version : '3'
      services :
        frontend :
	   image :  nginx
	   container_name : pathnex-frontend
	   ports :
	     - "80:80"
	   backend :
	      build: ./backend
	      container_name : pathnex-backend
	      ports : 
	         - "5000:5000"
	    database :
	       image : mysql:8
	       container_name : pathnex-mysql
	       environmet :
	         MYSQL_ROOT_PASSWORD : root

